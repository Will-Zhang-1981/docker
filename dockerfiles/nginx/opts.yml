images:
  latest: envygeeks/alpine:latest
  ubuntu: envygeeks/ubuntu:lts

tags:
  - latest
  - ubuntu

scripts:
  latest: apk --update add nginx-lua lua-socket
  ubuntu: apt-get update && apt-get install --no-install-recommends
    -y nginx-extras lua-socket
